# 🗄️ Database

Database layer for BYTE — schema definitions, migrations, and data access.

## Structure
- `migrations/` — Versioned schema migration files
- `schemas/` — Database schema definitions (SQL / ORM models)

## Tech
- SQLite (local development & desktop)
- PostgreSQL (cloud deployment)
