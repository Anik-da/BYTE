# 💾 Memory Module

Long-term memory and contextual awareness for BYTE.

## Structure
- `src/` — Memory management, retrieval, and indexing logic
- `store/` — Vector database and persistent storage files

## Tech
- ChromaDB / FAISS
- SQLite (structured memory)
- Sentence Transformers (embeddings)
