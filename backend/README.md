# ⚙️ Backend

API server for BYTE — handles routing, business logic, and data management.

## Structure
- `src/` — Routes, controllers, services, middleware
- `config/` — Environment and application configuration

## Tech
- Node.js + Express (REST API)
- Python + FastAPI (AI-facing endpoints)
- SQLite / PostgreSQL
